function typeError(var_name, type) {
    return 'The type of ' + var_name + 'is not correct, must be a ' + type;
}

function valueError(var_name) {
    return 'The value of ' + var_name + 'is not correct (too small or too big)';
}

